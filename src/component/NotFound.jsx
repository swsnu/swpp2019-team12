import React from 'react';

const NotFound = ({ context }) => {
    return (
        <div>
            <div>Not Found</div>
            <button>go to back</button>
        </div>
    );
};

export default NotFound;
