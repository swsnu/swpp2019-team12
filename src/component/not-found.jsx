import React from 'react';

const NotFound = ({ constext }) => {
    return (
        <div>
            <div>Not Found</div>
            <button>go to back</button>
        </div>
    );
};

export default NotFound;
