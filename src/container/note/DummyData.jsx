export const dummyNote = {
    note_title: 'Dummy Note',
    meeting_date: '01/01/2019'
};

export const participants = [
    {
        nickname: 'CHAEMIN',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'PAUL',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'andra',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'YEIN',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'JIN',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'FOO',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'BAR',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    },
    {
        nickname: 'SWPP',
        handleShowMember: () => {
            console.log('Need to Implement show member detail function');
        }
    }
];

export const handleAddAgendaBlock = note_id => {
    console.log(
        `Need to Implement adding Agenda Block to specific note whose id is ${note_id}`
    );
};
export const handleAddTextBlock = note_id => {
    console.log(
        `Need to Implement adding Text Block to specific note whose id is ${note_id}`
    );
};
export const handleAddImageBlock = note_id => {
    console.log(
        `Need to Implement adding Image Block to specific note whose id is ${note_id}`
    );
};
export const handleAddCalendarBlock = note_id => {
    console.log(
        `Need to Implement adding Calendar Block to specific note whose id is ${note_id}`
    );
};
export const handleAddPdfBlock = note_id => {
    console.log(
        `Need to Implement adding Pdf Block to specific note whose id is ${note_id}`
    );
};
export const handleAddTableBlock = note_id => {
    console.log(
        `Need to Implement adding Table Block to specific note whose id is ${note_id}`
    );
};
export const handleAddTodoBlock = note_id => {
    console.log(
        `Need to Implement adding Todo Block to specific note whose id is ${note_id}`
    );
};
